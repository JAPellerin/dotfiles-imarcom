## 0. Prérequis

- [x] 0.1 (références confirmées par l'utilisateur le 25 sept 2026 ; reste la vérification par `op`) Élément 1Password : `op read op://Imarcom/RocketChat/username` répond (identifiant attendu) et `op read op://Imarcom/RocketChat/password >/dev/null` réussit, depuis une session active — **vérifié le 25 sept 2026** : les deux références répondent.
- [x] 0.2 VM (snapshot « vierge », `base`, `1password`, `rocketchat`) : se connecter à la main dans le client et relever **pendant qu'il tourne** `~/.config/Rocket.Chat/config.json` (serveur, `userLoggedIn`), la commande du lanceur (`grep Exec /usr/share/applications/rocketchat*.desktop`, `command -v rocketchat-desktop`) ; consigner dans `design.md` (D7, D8) et corriger D7 si le signe diffère — **fait le 25 sept 2026** : fichier écrit pendant que le client tourne, `userLoggedIn: true`, URL avec `/` final ; aucune commande dans le `PATH`, `ROCKETCHAT_BIN=/opt/Rocket.Chat/rocketchat-desktop`

## 1. Module `rocketchat`

- [x] 1.1 `modules/61-rocketchat.sh` : `MODULE_DEPS="base 1password"` (D8) ; constantes `ROCKETCHAT_USER_REF`, `ROCKETCHAT_PASSWORD_REF`, `ROCKETCHAT_CONFIG` (surchargeable, `XDG_CONFIG_HOME`), `ROCKETCHAT_BIN` préfixé par `$ROCKETCHAT_ROOT` (D8) ; sonde `_rocketchat_logged_in` (D7 : requête `jq` tolérante, URL lue dans `config/rocketchat/servers.json`, `/` final ignoré) ; `module_check` l'exige ; `module_install` sans étape manuelle ; commentaires de `module_install` et de `module_check` réécrits (D9) ; consigne sans libellé d'interface (D8) ; `module_configure` lance `guided_login` après `servers.json` et le profil AppArmor, dans tous les cas (D8) ; en-tête : renvoi aux deux designs (`archive/2026-09-24-rocketchat`, D1 à D6, et ce change, D7 à D10) — les « Dn » existants gardent leur sens ; vérifier `shellcheck` propre et `./setup.sh --list` → « non disponible ici » dans la WSL
- [x] 1.2 `tests/test-rocketchat.sh` : assertion `MODULE_DEPS` contient `1password` (scénario « Lancé seul ») ; chargement de `lib/op.sh` et `lib/connexion.sh`, nouveaux cas de D10, `ROCKETCHAT_CONFIG` exporté vers le dossier du test, exécutable factice qui écrit `config.json`, cas existants adaptés (`config.json` « connecté » posé avant, assertions d'étape après `module_install` retirées, assertion de la ligne 99 réduite à `servers.json`) ; vérifier `bash tests/run-all.sh` vert et la ligne `shellcheck` de `CLAUDE.md` propre

## 2. Validation en VM et documentation

- [ ] 2.1 VM (snapshot « vierge ») : bootstrap → `base`, `1password`, `rocketchat` → le client s'ouvre sur `rocketchat.imarcom.net`, l'identifiant s'affiche, coller le mot de passe → « Rocket.Chat : fait (presse-papiers vidé) », `wl-paste` vide, mot de passe absent de `~/.local/state/dotfiles/setup-*.log` ; relance → « déjà fait », aucun appel à `op` ; se déconnecter dans le client → relever `userLoggedIn` : s'il passe à `false`, `module_check` à faire et relance → parcours reproposé sans téléchargement ; s'il reste `true`, le consigner (design, « Risks ») ; « Passer » → étape manuelle au résumé ; consigner ici
- [ ] 2.2 `ROADMAP.md` : vague 4, `rocketchat` fait (date), et ligne `rocketchat` du tableau des modules (connexion guidée par 1Password) ; au sync, réécrire le `## Purpose` de `openspec/specs/module-rocketchat/spec.md` (« n'ait plus qu'à se connecter » est périmé : un delta ne le touche pas) ; `openspec validate rocketchat-connexion --strict` vert

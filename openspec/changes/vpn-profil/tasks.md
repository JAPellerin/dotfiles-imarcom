## 0. Prérequis

- [x] 0.1 Élément 1Password (références confirmées par l'utilisateur le 25 sept 2026) : `op read op://Imarcom/VPN/username` répond, `op read op://Imarcom/VPN/password >/dev/null` et `op read op://Imarcom/VPN/jpellerin.ovpn | grep -c '^remote '` réussissent (profil joint à l'élément) ; sinon, joindre le profil ou corriger `VPN_OVPN_REF` dans `design.md` (D4) — **vérifié le 25 sept 2026** : les trois références répondent, `jpellerin.ovpn` joint à l'élément (lignes `client`, `remote`, `<key>` présentes).
- [x] 0.2 VM (snapshot « vierge », `base`, `vpn`) : depuis le terminal de la session graphique, importer un profil factice par `nmcli connection import` **sans sudo** (polkit), puis `nmcli connection edit` par l'entrée standard (`set vpn.secrets password = …`, `save`) ; relever les droits, l'emplacement des fichiers extraits par le greffon et la syntaxe acceptée ; consigner dans `design.md` (D6) et corriger si besoin — **fait le 25 sept 2026** : import, `modify` et `edit` sans sudo ni fenêtre depuis la session graphique (refusés par SSH) ; connexion stockée par netplan (`/etc/netplan/90-NM-<uuid>.yaml`) ; certificats dans `~/.local/share/networkmanagement/certificates/nm-openvpn/` ; échappement confirmé ; l'éditeur répète le mot de passe sur sa sortie → `/dev/null` obligatoire (D6)
- [x] 0.3 VM (snapshot « vierge », `base`, `vpn`), depuis le terminal de la session graphique, avec un profil factice portant `<ca>`, `<cert>`, `<key>` et `<tls-crypt>` : importer, `modify … autoconnect no … password-flags=0`, `edit` (mot de passe factice avec `,` et `\`) ; relever (a) la sortie exacte de `nmcli --show-secrets -g vpn.secrets connection show uuid <uuid>` **sans sudo** et son échappement ; (b) `nmcli -t -f PERMISSION,VALUE general permissions` (ligne `settings.modify.system`), dans la session graphique puis par `ssh vm` ; (c) les `.pem` extraits (noms, `tls-crypt` compris, droits) ; puis `nmcli connection delete uuid <uuid>` et constater l'absence de `/etc/netplan/90-NM-<uuid>.yaml` (`sudo ls`), de `/run/NetworkManager/system-connections/netplan-NM-<uuid>.nmconnection` et des `.pem` ; consigner dans `design.md` (Context, D6) et corriger D6 si `delete` laisse un fichier — **fait le 25 sept 2026** : UUID par différence exact ; `settings.modify.system` `yes` en session graphique, `auth` par SSH ; `-g` rend `password = ab\\,c\\d` pour `ab,c\d` (`,` échappé, `\` non, puis `\` doublés par `-g`) → contrôle par comparaison des formes échappées (D6) ; quatre `.pem` dont `tls-crypt`, `600 <utilisateur>` ; `delete` retire YAML et fichier sous `/run` mais **pas** les `.pem` (retirés par `_vpn_abandon`)

## 1. Module `vpn`

- [ ] 1.1 `modules/65-vpn.sh` : constantes de D4, `MODULE_DESC` (D8), import dans `module_configure` (D4 à D7) — fichier temporaire privé avec nettoyage enregistré avant l'écriture, autorisation polkit vérifiée avant toute lecture, profil lu avant l'identifiant et le mot de passe, erreurs de lecture au journal, UUID par différence des listes avant et après l'import (jamais par la sortie de l'import), garde `_vpn_abandon` enregistrée dès l'UUID connu (connexion et `Imarcom-*.pem` retirés sur échec ou interruption), identifiant et mot de passe échappés, `modify` avant `edit`, mot de passe par l'entrée standard, contrôle par comparaison du secret relu au mot de passe attendu (D6) ; commentaires devenus faux réécrits (D8) ; en-tête : renvoi aux deux designs (`archive/2026-09-24-vpn`, D1 à D3, et ce change, D4 à D9) — les « Dn » existants gardent leur sens ; vérifier `shellcheck` propre et `./setup.sh --list` → « non disponible ici » dans la WSL
- [ ] 1.2 `tests/test-vpn.sh` : chargement de `lib/op.sh`, doublure `op` explicite, doublure `nmcli` étendue (`general permissions`, `.pem` factices, `--show-secrets`), `VPN_CERT_DIR` du test, cas de D9 (dont interruption après l'import, autorisation refusée, profil illisible sans lecture du mot de passe, secret enregistré différent, WireGuard « Imarcom » déjà présente), cas existants adaptés (étape d'import par « sans session ») ; vérifier `bash tests/run-all.sh` vert et la ligne `shellcheck` de `CLAUDE.md` propre

## 2. Validation en VM et documentation

- [ ] 2.1 VM (snapshot « vierge ») : bootstrap → `base`, `1password`, `vpn` → connexion « Imarcom » dans Paramètres > Réseau > VPN, inactive ; l'activer depuis le menu système → VPN établi **sans invite** (adresse du tunnel, `nmcli connection show --active`) ; `sudo grep -c 'vpn-secrets.password' /etc/netplan/90-NM-<uuid>.yaml` ; `sudo stat -c '%a %U' /etc/netplan/90-NM-<uuid>.yaml /run/NetworkManager/system-connections/netplan-NM-<uuid>.nmconnection` → `600 root` ; `stat -c '%a %U' ~/.local/share/networkmanagement/certificates/nm-openvpn/Imarcom-*.pem` → `600 <utilisateur>` ; aucun `.ovpn` restant dans `$XDG_RUNTIME_DIR` ni `/tmp` ; ni le profil ni le mot de passe dans `~/.local/state/dotfiles/setup-*.log` ; redémarrage → VPN inactif ; relance → « déjà fait », aucun appel `op` ; consigner ici
- [ ] 2.2 `ROADMAP.md` : vague 4, `vpn` fait (date ; reconnexion automatique et commande `vpn` toujours reportées), et ligne 65 du tableau des modules (« profil reporté » → profil importé depuis 1Password) ; au sync, réécrire le `## Purpose` de `openspec/specs/module-vpn/spec.md` (« la configuration du profil reste manuelle… » est périmé : un delta ne le touche pas) ; `openspec validate vpn-profil --strict` vert

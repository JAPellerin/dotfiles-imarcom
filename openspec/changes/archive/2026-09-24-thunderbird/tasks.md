## 1. Module `thunderbird`

- [x] 1.1 `config/thunderbird/thunderbird.desktop` : le fichier `.desktop` publié par Mozilla (sumo-kb), `Exec` et `Icon` portant `@THUNDERBIRD_DIR@` (D3) ; vérifier `desktop-file-validate` sur une version rendue, s'il est disponible
- [x] 1.2 `modules/62-thunderbird.sh` (`MODULE_GROUP=apps`, `MODULE_DEPS="base shell"`, `MODULE_NEEDS_GUI=1`) — en-tête avec le lien de la doc de Mozilla, constantes, `module_check` (D4), `module_install` (D1, étape des comptes), `module_configure` (D2, D3) ; vérifier `shellcheck` propre et `./setup.sh --list` → « non disponible ici » dans la WSL
- [x] 1.3 `tests/test-thunderbird.sh` : les cas de D5 ; vérifier `bash tests/run-all.sh` vert et la ligne `shellcheck` de `CLAUDE.md` propre

- [x] 1.4 Langue d'Ubuntu (D6, ajouté le 24 sept 2026) : langues publiées en constante, `_thunderbird_wanted_lang`, `_thunderbird_installed_lang` (`omni.ja`), `@LANG@` dans `THUNDERBIRD_URL`, langue de l'archive vérifiée, réinstallation par échange ; `module_check` sur la langue ; cas de D5 ; vérifier `bash tests/test-thunderbird.sh` vert
- [x] 1.5 Dictionnaires (D7, ajouté le 24 sept 2026) : `config/thunderbird/policies.json`, copié par `install_system_file` vers `$THUNDERBIRD_ETC/etc/thunderbird/policies/policies.json` dans `module_configure`, constaté par `module_check` ; cas de D5 ; vérifier `bash tests/run-all.sh` vert et la ligne `shellcheck` de `CLAUDE.md` propre

## 2. Validation en VM et documentation

- [x] 2.1 VM (snapshot « vierge ») : bootstrap → `base`, `1password`, `thunderbird` → Thunderbird s'ouvre depuis le menu, en français, avec son icône ; `~/.local/share/thunderbird` appartient à l'utilisateur ; **Aide > À propos : la recherche de mise à jour fonctionne** (pas de « mise à jour impossible ») ; aucun snap ni paquet `thunderbird` installé ; comparer D2 à la méthode « dossier personnel » de la page de Mozilla (navigateur de la VM) ; résumé : étape des comptes ; relance → « déjà fait », aucun téléchargement ; **revalidation D6/D7 (24 sept 2026)**, dans la VM déjà installée en `fr` avec Ubuntu en `en_US.UTF-8` : `./setup.sh thunderbird` → réinstallation annoncée, version `en-US` (`unzip -p ~/.local/share/thunderbird/omni.ja res/multilocale.txt`), profil `~/.config/thunderbird` et comptes intacts, `/etc/thunderbird/policies/policies.json` identique au dépôt ; Thunderbird s'ouvre en anglais ; **`about:policies` montre `ExtensionSettings` actif** (sinon repli de D7) ; Modules complémentaires → Dictionnaires : « Canadian English Dictionary » et « Dictionnaire français » installés ; rédaction d'un courriel : les deux proposés à la vérification orthographique ; relance → « déjà fait », aucun `sudo` ; consigner ici
  - Constaté le 24 sept 2026 (VM, bootstrap depuis main `810e6ab`, puis `ad96fa8` pour D6 et D7) : Thunderbird 156.0.1 s'ouvre depuis le menu avec son icône, d'abord en français ; `~/.local/share/thunderbird` appartient à l'utilisateur ; Aide > À propos : la recherche de mise à jour fonctionne ; ni snap ni paquet `thunderbird` ; résumé : étape des comptes ; relance → « déjà fait », aucun téléchargement. D6 : Ubuntu en `en_US.UTF-8` → relance « à faire », réinstallé en `en-US` (`res/multilocale.txt` : `en-US`), profil `~/.config/thunderbird` et comptes intacts, relance → « déjà fait » sans `sudo`. D7 : **`/etc/thunderbird/policies/policies.json` est bien lu** — au lancement suivant, `en-CA@dictionaries.addons.mozilla.org` 3.1.3 et `fr-dicollecte@dictionaries.addons.mozilla.org` 6.3.1webext installés dans le profil et actifs (`extensions.json`) ; le repli `distribution/` est inutile. Profil créé par Thunderbird dans `~/.config/thunderbird` (plus `~/.thunderbird`). Confirmé par l'utilisateur après la réinstallation : Thunderbird s'ouvre **en anglais**, et à la rédaction d'un courriel la vérification orthographique propose **les deux dictionnaires** (anglais (Canada) et français) — scénario « Premier démarrage ». D2 comparé à la méthode « dossier personnel » de Mozilla (page lue dans la copie de la Wayback Machine du 7 mai 2026, la page en direct n'étant rendue qu'en JavaScript) : **conforme**, rien à corriger (relevé dans le Context du design).
- [x] 2.2 `ROADMAP.md` : `thunderbird` fait (date, version) ; `openspec validate thunderbird --strict` vert

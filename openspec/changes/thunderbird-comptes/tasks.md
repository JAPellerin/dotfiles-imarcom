## 0. Prérequis

- [x] 0.1 1Password (par l'utilisateur) : créer `op://Imarcom/Thunderbird` selon D8 — champs `nom` et `adresse`, signature dans la note, section `agendas` avec un champ par agenda (les 8 retenus le 25 sept 2026 : ceux de l'actuel moins un agenda de collègue retiré) — (mot de passe Google Workspace : `op://Imarcom/Google Workspace/password`, nommé le 25 sept 2026) ; vérifier `op read op://Imarcom/Thunderbird/adresse`, `op read op://Imarcom/Thunderbird/agendas/1` et `op read 'op://Imarcom/Google Workspace/password' >/dev/null` — **fait le 25 sept 2026** : élément créé (note sécurisée) depuis le profil Windows et les courriels envoyés — nom renseigné, signature HTML avec logo validée par l'utilisateur, 8 agendas (`agendas/1` à `agendas/8`, pas de `agendas/9`) ; `op read` des champs et du mot de passe Google Workspace vérifiés ; signature relue identique à celle validée.
- [x] 0.2 VM (snapshot « vierge », `base`, `shell`, `thunderbird`) : `~/.local/share/thunderbird/thunderbird --headless -CreateProfile default-release`, puis premier lancement normal → Thunderbird ouvre **ce** profil (pas de second dossier dans `~/.config/thunderbird`, `installs.ini` le désigne) ; y ajouter à la main le compte de travail en OAuth2 et un agenda Google → relever `authMethod`, les préférences écrites, **le moment** où `logins.json` reçoit `oauth://accounts.google.com` (Thunderbird ouvert ?), le nombre d'autorisations demandées (courriel et agendas) et la création de « Local Folders » et « Home » ; consigner dans `design.md` (D9, D11, D12, D13) et corriger avant le code — **fait le 25 sept 2026** : `-CreateProfile` **non adopté** (Thunderbird crée un second profil) → D9 passe au premier lancement sans fenêtre, vérifié (≈ 1 s, profil repris depuis le menu) ; `authMethod 10` pour IMAP et SMTP, préférences conformes à D11 ; `logins.json` écrit Thunderbird ouvert, une seule autorisation (courriel, contacts, agendas) ; « Home » créé par Thunderbird ; « Local Folders » à constater à la validation
- [x] 0.3 VM : sur un profil sans compte, ouvrir Thunderbird depuis le menu puis **fermer l'assistant de compte** sans rien créer → relever dans `prefs.js` ce qu'il reste (`mail.accountmanager.accounts`, serveur `none` de « Local Folders ») ; confirme la règle de D11 — **fait le 25 sept 2026** : aucun « Local Folders » ni préférence de compte après l'assistant fermé, seulement l'agenda « Home » ; la règle de D11 reste pour le cas « compte supprimé par l'utilisateur »
- [x] 0.4 WSL, session 1Password active : relever le message d'erreur exact de `op read` pour un champ absent (`op read op://Imarcom/Thunderbird/agendas/99 2>&1 >/dev/null`) et pour un élément absent ; reporter le texte reconnu dans `THUNDERBIRD_OP_NOT_FOUND` (D8) — **fait le 25 sept 2026** (`op` 2.39.0) : champ ou section absents → `… item 'Imarcom/Thunderbird' does not have a field 'agendas.9'` ; élément absent → `… isn't an item in the "Imarcom" vault …` ; coffre absent → `… isn't a vault in this account …` ; sans session → `… You are not currently signed in …` ; code de sortie 1 dans tous les cas

## 1. Module `thunderbird`

- [ ] 1.1 `config/thunderbird/compte.js` : gabarit de D11, sans donnée personnelle ni jeton en toutes lettres dans le commentaire d'en-tête ; vérifier `grep -c 'authMethod", 10' config/thunderbird/compte.js` → 2 et qu'aucune adresse `@imarcom.net` n'y figure
- [ ] 1.2 `modules/62-thunderbird.sh` : `MODULE_DEPS="base shell 1password"` ; constantes (références 1Password, `THUNDERBIRD_PROFILES` surchargeable, `THUNDERBIRD_WORK_DOMAIN`, dossiers Gmail datés, délais `THUNDERBIRD_*_TIMEOUT` / `_POLL_INTERVAL`, `THUNDERBIRD_OP_NOT_FOUND`) ; `_thunderbird_profile` (racine XDG ou `~/.thunderbird`, une seule section d'installation, `IsRelative`) et premier lancement sans fenêtre lancé directement, SIGTERM puis mort du processus puis `prefs.js` exigé, KILL au-delà (D9) ; `_thunderbird_running` et étape `THUNDERBIRD_CLOSE_MANUAL` sans question (D10) ; lecture de l'élément avec fin de liste reconnue (D8) ; rendu du bloc : échappement, `@SIGNATURE@` en dernier, `%` encodé d'abord, `sig_bottom false`, `calendar-main-default` sur l'agenda de l'adresse, `calendar.list.sortOrder`, clés libres, `accounts` et `smtpservers` fusionnés, écriture atomique (D11) ; `_thunderbird_write_account` aux codes 0/1/2 dans l'ordre de D13 (autre compte avant 1Password, 1Password avant le profil) ; sondes `_thunderbird_account_present` (domaine `@imarcom.net`) et `_thunderbird_google_connected` dans `module_check` (D12) ; `THUNDERBIRD_LOGIN_MANUAL` ; `module_configure` : écriture puis `guided_login` (D13) ; `module_install` sans étape des comptes ; `MODULE_DESC` ; commentaires devenus faux réécrits (D13) ; en-tête : renvoi aux deux designs (`archive/2026-09-24-thunderbird`, D1 à D7, et ce change, D8 à D14) — les « Dn » existants gardent leur sens ; vérifier `shellcheck` propre et `./setup.sh --list` → « non disponible ici » dans la WSL
- [ ] 1.3 `tests/test-thunderbird.sh` : chargement de `lib/op.sh` et `lib/connexion.sh`, `has_gui` forcé, délais courts, `thunderbird` factice étendu à `--headless` (trap qui tue son `sleep`), cas de D14, cas existants adaptés (profil « fait » posé avant les appels à `module_configure` qui ne portent pas sur le compte, assertion d'étape après `module_install` retirée) ; vérifier `bash tests/run-all.sh` vert et la ligne `shellcheck` de `CLAUDE.md` propre

## 2. Validation en VM et documentation

- [ ] 2.1 VM (snapshot « vierge ») : bootstrap → `base`, `1password`, `shell`, `thunderbird` → « Compte Google de travail écrit … (8 agendas) » ; Thunderbird s'ouvre **sans assistant de compte**, « Local Folders » présent, **nombre de fenêtres de connexion Google** au premier démarrage (IMAP, SMTP et 8 agendas démarrent ensemble) consigné, agendas dans l'ordre des champs, **agenda par défaut** = celui de l'adresse de travail (« Home » ne l'a pas repris), la **fenêtre principale** de Thunderbird paraît-elle avant la fenêtre de connexion Google (consigne à ajuster sinon), avertissement éventuel de Mozilla sur la taille de la signature (> 4 Ko) consigné, adresse affichée, mot de passe collé dans la fenêtre Google, double authentification → « Thunderbird (Google) : fait (presse-papiers vidé) » ; courriels chargés, dossiers Gmail reconnus (Envoyés, Brouillons, Archives, Corbeille), envoi d'un courriel de test par `smtp.gmail.com`, signature HTML avec logo (visible chez le destinataire, sans pièce jointe séparée), placée sous la réponse et au-dessus de la citation, réponse au-dessus, 8 agendas avec leurs couleurs (collègues en lecture seule), dictionnaires toujours là ; mot de passe, signature et agendas absents de `~/.local/state/dotfiles/setup-*.log` ; relance → « déjà fait », aucun appel `op` ; changer la signature dans Thunderbird, relance → conservée ; Thunderbird ouvert + profil sans compte (profil retiré) → aucune question, étape « fermer Thunderbird et relancer » au résumé ; relance Thunderbird fermé → compte écrit ; consigner ici
- [ ] 2.2 `ROADMAP.md` : vague 4, `thunderbird` fait (date) — et corriger « les 9 agendas Google » en 8 ; `openspec validate thunderbird-comptes --strict` vert

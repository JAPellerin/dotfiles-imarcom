## 0. Prérequis

- [x] 0.1 1Password (par l'utilisateur) : créer `op://Imarcom/Thunderbird` selon D1 — champs `nom` et `adresse`, signature dans la note, section `agendas` avec un champ par agenda (les 8 retenus le 25 sept 2026 : ceux de l'actuel moins un agenda de collègue retiré) — (mot de passe Google Workspace : `op://Imarcom/Google Workspace/password`, nommé le 25 sept 2026) ; vérifier `op read op://Imarcom/Thunderbird/adresse`, `op read op://Imarcom/Thunderbird/agendas/1` et `op read 'op://Imarcom/Google Workspace/password' >/dev/null` — **fait le 25 sept 2026** : élément créé (note sécurisée) depuis le profil Windows et les courriels envoyés — nom `Joseph-Antony Pellerin`, signature HTML avec logo validée par l'utilisateur, 8 agendas (`agendas/1` à `agendas/8`, pas de `agendas/9`) ; `op read` des champs et du mot de passe Google Workspace vérifiés ; signature relue identique à celle validée.
- [ ] 0.2 VM (snapshot « vierge », `base`, `shell`, `thunderbird`) : `~/.local/share/thunderbird/thunderbird --headless -CreateProfile default-release`, puis premier lancement normal → Thunderbird ouvre **ce** profil (pas de second dossier dans `~/.config/thunderbird`, `installs.ini` le désigne) ; y ajouter à la main le compte de travail en OAuth2 et un agenda Google → relever `authMethod`, les préférences écrites, **le moment** où `logins.json` reçoit `oauth://accounts.google.com` (Thunderbird ouvert ?), le nombre d'autorisations demandées (courriel et agendas) et la création de « Local Folders » et « Home » ; consigner dans `design.md` (D2, D4, D5, D6) et corriger avant le code

## 1. Module `thunderbird`

- [ ] 1.1 `config/thunderbird/compte.js` : gabarit de D4, sans donnée personnelle ni jeton en toutes lettres dans le commentaire d'en-tête ; vérifier `grep -c 'authMethod", 10' config/thunderbird/compte.js` → 2 et qu'aucune adresse `@imarcom.net` n'y figure
- [ ] 1.2 `modules/62-thunderbird.sh` : constantes (références 1Password, `THUNDERBIRD_PROFILES` surchargeable) ; `_thunderbird_profile` et création du profil (D2) ; `_thunderbird_running` (D3) ; lecture de l'élément et rendu du bloc avec échappement (D1, D4) ; `_thunderbird_write_account` aux codes 0/1/2 (D6) ; sondes `_thunderbird_account_present` et `_thunderbird_google_connected` dans `module_check` (D5) ; `module_configure` : écriture puis `guided_login` (D6) ; `module_install` sans étape des comptes ; `MODULE_DESC` et en-tête mis à jour (renvoi à ce change) ; vérifier `shellcheck` propre et `./setup.sh --list` → « non disponible ici » dans la WSL
- [ ] 1.3 `tests/test-thunderbird.sh` : cas de D7, cas existants adaptés ; vérifier `bash tests/run-all.sh` vert et la ligne `shellcheck` de `CLAUDE.md` propre

## 2. Validation en VM et documentation

- [ ] 2.1 VM (snapshot « vierge ») : bootstrap → `base`, `1password`, `shell`, `thunderbird` → « Compte Google de travail écrit … (8 agendas) » ; Thunderbird s'ouvre **sans assistant de compte**, adresse affichée, mot de passe collé dans la fenêtre Google, double authentification → « Thunderbird (Google) : fait (presse-papiers vidé) » ; courriels chargés, dossiers Gmail reconnus (Envoyés, Brouillons, Archives, Corbeille), envoi d'un courriel de test par `smtp.gmail.com`, signature HTML avec logo (visible chez le destinataire, sans pièce jointe séparée) et réponse au-dessus, 8 agendas avec leurs couleurs (collègues en lecture seule), dictionnaires toujours là ; mot de passe, signature et agendas absents de `~/.local/state/dotfiles/setup-*.log` ; relance → « déjà fait », aucun appel `op` ; changer la signature dans Thunderbird, relance → conservée ; Thunderbird ouvert + profil sans compte (profil retiré) → demande de le fermer, « Passer » → étape manuelle ; consigner ici
- [ ] 2.2 `ROADMAP.md` : vague 4, `thunderbird` fait (date) ; `openspec validate thunderbird-comptes --strict` vert

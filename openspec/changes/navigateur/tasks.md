## 1. Socle

- [x] 1.1 `lib/files.sh` : `install_system_file <source> <cible> [mode]` (D6 : source relative au dépôt ou absolue, `cmp -s` puis `run_sudo install -m <mode> -D`, 0 si écrit / 1 si identique / échec nommé si source absente) ; cas dans `tests/test-files.sh` avec `fake_sudo` : création avec dossiers parents, réécriture seulement si contenu différent, mode appliqué, source introuvable
- [x] 1.2 `lib/apt.sh` : `apt_install_pinned <paquet...>` (D3 : `apt-get install` inconditionnel sous `ui_spin`, `apt_update_once` avant) et `apt_remove <paquet...>` (paquets présents seulement, 0 si rien à faire) ; cas dans `tests/test-apt.sh` : `apt_install_pinned` appelle `apt-get install` même si le paquet est installé, `apt_remove` n'appelle `apt-get remove` que pour les paquets présents ; `shellcheck` propre

## 2. Fichiers versionnés

- [x] 2.1 `config/navigateur/chromium-1password.json` (ExtensionSettings, `normal_installed`, `update_url` du Chrome Web Store), `config/navigateur/firefox-policies.json` (`policies.ExtensionSettings` avec l'ID AMO et `install_url` `latest.xpi`), `config/navigateur/mozilla.pref` (épinglage `Pin-Priority: 1000`), `config/navigateur/google-chrome.default` (`repo_add_once="false"`, `repo_reenable_on_distupgrade="false"`), `config/navigateur/bip39-english.txt` (liste officielle BIP-0039, `bitcoin/bips`, 2048 lignes, avec la source en commentaire dans le module) ; vérifier `jq . config/navigateur/*.json` et `wc -l` = 2048

## 3. Module `navigateur`

- [ ] 3.1 `modules/25-navigateur.sh` (`MODULE_GROUP=apps`, `MODULE_DEPS="base 1password"`, `MODULE_NEEDS_GUI=1`, `module_check` toujours 1 avec commentaire D7) — table des navigateurs (D1), questions au début de `module_install` (`ui_choose_multi` précochée avec les navigateurs installés, langue de Firefox à trois options « Comme Ubuntu (<code>) / Français / Anglais » selon D3, navigateur par défaut selon D2 ; Chrome masqué hors `amd64`), sélection vide → rien à faire ; vérifier avec `./setup.sh --list` que le module est « non disponible ici » dans la WSL et que `module_meta` l'accepte
- [ ] 3.2 Installation : Brave (`apt_add_repo brave-browser`, clé `.gpg`), Firefox (`apt_add_repo mozilla` clé `.asc`, `install_system_file` de l'épinglage, `apt_install_pinned firefox` si absent ou version `snap`, langue résolue en `fr` → `apt_install firefox-l10n-fr`, `en` → `apt_remove firefox-l10n-fr`, autre → avertissement, `snap remove firefox` après le `.deb` seulement), Chrome (`install_system_file` de `/etc/default/google-chrome` avant `apt_add_repo google-chrome` + `apt_install google-chrome-stable`) ; puis navigateur par défaut via `xdg-settings` (D2) ; vérification par les tests de 3.5
- [ ] 3.3 `module_configure` : stratégie 1Password pour chaque navigateur installé (D5 : `install_system_file` vers `/etc/brave/policies/managed/1password.json`, `/etc/opt/chrome/policies/managed/1password.json`, `/etc/firefox/policies/policies.json` généré par `jq` avec `intl.locale.requested=fr` si `firefox-l10n-fr` est installé), message « déjà en place » quand rien ne change ; préfixe `NAV_ETC` surchargeable pour les tests
- [ ] 3.4 Brave Sync guidé (D9) : `_nav_brave_word25` (fonction pure, `NAV_NOW` surchargeable, liste BIP39 du dépôt), détection de la chaîne dans `Preferences` (`NAV_BRAVE_PREFS` surchargeable), `op_read` de `op://Private/Brave Sync/code` (24 premiers mots ; échec → avertissement + `manual_step`, code 0), `apt_install wl-clipboard`, phrase dans `wl-copy` sans passer par `run`, `brave-browser brave://settings/braveSync/setup` détaché, consigne, `ui_wait` 300 s puis menu « Continuer d'attendre / Passer », `wl-copy --clear` à la réussite ; rien si la chaîne est déjà rejointe ; vérifier le 25ᵉ mot du jour contre `alexeybarabash.github.io/brave-sync-word-25` (et un autre appareil Brave)
- [ ] 3.5 `tests/test-navigateur.sh` selon D8 (doublures `dpkg-query`, `snap`, `xdg-settings`, `gum`, `apt-get`, `curl`, `op`, `wl-copy`, `brave-browser`, `fake_sudo`) : tous les cas listés (installation, langue à trois options, stratégies, Brave Sync dont le 25ᵉ mot sur deux dates et l'absence de la phrase dans le journal) ; `bash tests/run-all.sh` vert, `shellcheck` propre (ajouter le test et le module aux cibles listées dans `CLAUDE.md` si le motif ne les couvre pas)

## 4. Documentation et VM

- [ ] 4.1 `ROADMAP.md` (`navigateur` fait, contenu réel : dépôts officiels, snap remplacé, extension par stratégie), `CLAUDE.md` (stratégies d'entreprise comme mécanisme de pré-installation d'extensions ; `install_system_file`, `apt_install_pinned`, `apt_remove` dans la liste des helpers ; secret `op://Private/Brave Sync/code`) ; `openspec validate navigateur --strict` vert
- [ ] 4.2 VM (snapshot « vierge ») : bootstrap → `base`, `1password`, `navigateur` (Brave + Firefox « Français », défaut Brave) → vérifier `brave-browser --version`, `firefox --version` (Mozilla), `snap list firefox` en échec, `apt-cache policy firefox` (candidat `packages.mozilla.org`), `xdg-settings get default-web-browser` = `brave-browser.desktop`, `xdg-open https://github.com` ouvre Brave, `brave://policy` et `about:policies` listent l'extension, extension 1Password présente et déverrouillable par l'app, Firefox en français ; Brave Sync : avant l'étape, relever la clé de `Preferences` qui change quand la chaîne est rejointe (confirmer `brave_sync_v2.seed`, D9), coller le code calculé → chaîne rejointe, presse-papiers vidé ; relance → question précochée, rien réinstallé, aucune stratégie réécrite, aucune étape sync ; noter les constats (D3 déclassement, D4 `google-chrome.list`, D9 clé) dans `design.md`
- [ ] 4.3 VM, second passage : `navigateur` avec Chrome ajouté → `google-chrome-stable` installé, `google-chrome.sources` seul (pas de `google-chrome.list`, sinon appliquer le secours D4), question du navigateur par défaut non posée (défaut déjà dans la sélection), stratégie Chrome écrite ; puis `git` → `gh auth login` dans Brave avec l'extension 1Password
